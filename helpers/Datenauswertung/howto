
Testergebnisse filtern und einzelne Dateien schreiben
Das Ergebnis ist ein Ordner mit einzelnen Dateien, die je die Werte für minor faults, tlb store und load misses beinhalten.
./extract_data <testdata_raw> 
Anschließend können die einzelnen Dateien nach beliebigen zu CSV Dateien zusammengefügt werden.

get_average.py gibt die Durchschnittswerte aus, der Pfad zur CSV Datei und der Name müssen innerhalb der Datei spezifiziert werden. Namenskonventionen müssen beachtet werden. 
Sollte nach Bedarf angepasst werden, wenn etwa eine CSV nicht alle Messaspekte beinhaltet. 



